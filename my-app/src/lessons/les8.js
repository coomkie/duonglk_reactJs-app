import React from 'react';

const Les8 = () => {
    function hello(name, age) {
        console.log("hello " + name + " - " + age);
    };
    hello("duong", 20);
    return (

        <div>

        </div>
    );
};

export default Les8;